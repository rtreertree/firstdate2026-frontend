name: Deploy to Cloudflare

on:
  push:
    branches:
      - main
      - dev
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: deploy-${{ github.ref }}
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    name: Build and Deploy
    environment:
      name: ${{ github.ref_name == 'main' && 'production' || 'development' }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Type check
        run: bun run typecheck

      - name: Lint
        run: bun run lint

      - name: Check formatting
        run: bun run format:check

      - name: Build
        env:
          PUBLIC_API_BASE_URL: ${{ secrets.PUBLIC_API_BASE_URL }}
        run: bun run build

      # --- MAIN BRANCH -> firstdate2026-frontend (www.cufirstdate2026.com) ---
      - name: Deploy to Production
        if: github.ref_name == 'main'
        uses: cloudflare/wrangler-action@v4
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          command: deploy
          packageManager: bun

      # --- DEV BRANCH -> firstdate2026-frontend-staging (staging.cufirstdate2026.com) ---
      - name: Deploy to Staging
        if: github.ref_name == 'dev'
        uses: cloudflare/wrangler-action@v4
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          command: deploy --env staging
          packageManager: bun

      - name: Print Deployed URL
        run: |
          if [ "${{ github.ref_name }}" = "main" ]; then
            echo "🚀 Production deployed: https://www.cufirstdate2026.com"
          else
            echo "🚀 Staging deployed: https://staging.cufirstdate2026.com"
          fi
